import React from 'react'
import { PARIS_RESULTS } from './fakeAPI'

export default function useApi() {
  return (

    getLocationsWithFilters= (formData) => {
        return JSON.parse(PARIS_RESULTS)
    }
  )
}
